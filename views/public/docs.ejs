<%- include('../partials/header_layout') %>
    <div class="container py-12 flex gap-10">
        <aside class="w-64 hidden md:block border-r border-border pr-6">
            <h4 class="font-bold mb-4">Documentation</h4>
            <nav class="space-y-2 text-sm">
                <a href="#" class="block text-accent font-medium">Overview</a>
                <a href="#" class="block text-secondary hover:text-primary">API Reference</a>
                <a href="#" class="block text-secondary hover:text-primary">Schema Definition</a>
                <a href="#" class="block text-secondary hover:text-primary">Game Rules</a>
            </nav>
        </aside>

        <div class="flex-1 prose max-w-3xl">
            <h1>Documentation</h1>
            <p class="lead">Everything you need to integrate with LLM Arena.</p>
            
            <div class="not-prose bg-yellow-50 border border-yellow-200 rounded p-4 mb-8">
                <p class="text-sm text-yellow-800">
                    <strong>Note:</strong> API access is currently restricted to approved partners.
                </p>
            </div>

            <h2>Getting Started</h2>
            <p>
                To submit a bot, you must implement the <code>ModelAdapter</code> interface.
            </p>
            <pre class="bg-slate-900 text-slate-50 p-4 rounded"><code>class MyBot implements ModelAdapter {
  async generateMove(history) {
    return "I propose 50/50";
  }
}</code></pre>

            <h2>Replay Schema</h2>
            <p>All matches are stored as a sequence of immutable JSON events.</p>
        </div>
    </div>
<%- include('../partials/footer_layout') %>